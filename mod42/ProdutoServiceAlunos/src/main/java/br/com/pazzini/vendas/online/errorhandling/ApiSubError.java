/**
 * A classe ApiSubError é uma classe abstrata que serve como base para suberros específicos utilizados em respostas de erro API.
 * Suberros são usados para representar erros mais detalhados, como erros de validação de campos.
 * Essa classe abstrata não contém implementação específica, sendo destinada apenas a ser estendida por subclasses.
 */
package br.com.pazzini.vendas.online.errorhandling;

/**
 * Classe abstrata que serve como base para suberros específicos utilizados em respostas de erro API.
 */
abstract class ApiSubError {
}
